# Usage

```
# 実行に時間がかかったスペックを出力
$ rspec --profile

# 前回失敗したスペックのみ実行
$ rspec --only-failures

# seed値を指定して実行
$ rspec --seed 724

# 失敗した時点で実行を停止
$ rspec --fail-fast

# テストを実行せずにテスト一覧を出力する
$ rspec --dry-run
```
